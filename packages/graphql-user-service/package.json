{
  "name": "@lazy-graphql/graphql-user-service",
  "version": "1.0.0",
  "description": "Graphql User Service",
  "scripts": {
    "start": "node ./dist",
    "watch": "tsc -w --incremental",
    "dev": "nodemon -r dotenv/config --watch ./dist --inspect=0.0.0.0:9229 --nolazy ./dist/index.js",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "@lazy-graphql/shared": "1.0.1"
  },
  "devDependencies": {
    "@types/node": "^12.12.12",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.1",
    "typescript": "^3.7.2"
  },
  "author": "Vittorio Adamo",
  "license": "ISC"
}
