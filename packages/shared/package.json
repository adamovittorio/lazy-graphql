{
  "name": "@lazy-graphql/shared",
  "version": "1.0.0",
  "description": "Resusable modules and types shared accross the lazy-graphql services",
  "main": "dist/index",
  "types": "dist/index",
  "files": [ "dist" ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^3.7.2"
  },
  "author": "Vittorio Adamo",
  "license": "ISC"
}